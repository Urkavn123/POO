/**
 * Enum Direction representa la dirección en la que puede moverse el ascensor.
 * El enum facilita el control del movimiento del ascensor, evitando el uso de valores numericos o cadenas las cuales dificultan el codigo
 */
public enum Direction{
    UP,
    DOWN
    }
